# PageQuill

PageQuill is a desktop app scaffold for writing and publishing static blog posts to GitHub-hosted repositories.

## Development

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
pytest
```
